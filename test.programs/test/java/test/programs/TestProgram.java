package test.programs;

/** Program with a class name that matches JUnit Platform Console's default class name filter. */
public class TestProgram {
  public static void main(String[] args) {
    assert args.length == 0;
  }
}
